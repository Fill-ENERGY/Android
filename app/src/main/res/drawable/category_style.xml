<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white"/>
    <corners
        android:radius="6.5dp"/>
    <stroke
        android:width="1dp"
        android:color="@color/gray_scale6"/>
    <solid android:color="@color/gray_scale3" /> <!-- 배경색 -->
</shape>
